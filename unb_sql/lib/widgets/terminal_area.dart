import 'package:flutter/material.dart';

class TerminalArea extends StatelessWidget {
  const TerminalArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
